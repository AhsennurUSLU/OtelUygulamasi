
@{
    Layout = null;
}
<!DOCTYPE html>

<html lang="en"
      class="light-style layout-menu-fixed"
      dir="ltr"
      data-theme="theme-default"
      data-assets-path="/adminPanel/assets/"
      data-template="vertical-menu-template-free">

@await Html.PartialAsync("~/Views/AdminLayoutPartial/_AdminHeaderPartial.cshtml")
<body>
    <!-- Layout wrapper -->
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <!-- Menu -->
            @await Html.PartialAsync("~/Views/AdminLayoutPartial/_AdminMenuPartial.cshtml")
            <!-- / Menu -->
            <!-- Layout container -->
            <div class="layout-page">
                <!-- Navbar -->
                @await Html.PartialAsync("~/Views/AdminLayoutPartial/_AdminNavbarPartial.cshtml")

                <!-- / Navbar -->
                <!-- Content wrapper -->
              
                <!-- Content wrapper -->
            </div>
            <!-- / Layout page -->
        </div>

        <!-- Overlay -->
        <div class="layout-overlay layout-menu-toggle"></div>
    </div>    <!-- / Layout wrapper -->
   

    <!-- Scripts -->
    @await Html.PartialAsync("~/Views/AdminLayoutPartial/_AdminScriptsPartial.cshtml")
</body>
</html>

@RenderBody()